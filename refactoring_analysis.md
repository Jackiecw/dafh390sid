# 销售数据管理重构方案分析报告

## 1. 方案总结
该方案旨在将销售数据录入从“按天汇总”升级为“订单级明细”，以提高数据颗粒度和准确性。核心内容包括：
*   **目标**：实现基于订单的自动化录入，支持退货逻辑和多链接（Listing）归因。
*   **数据库变更**：`SalesData` 表新增 `platformOrderId` (唯一索引), `orderStatus`, `quantity`, `unitPrice`。
*   **数据源处理**：
    *   **Shopee**：过滤无效订单，使用 `Total Harga Produk` 作为销售额，通过 SKU 或 标题 匹配。
    *   **TikTok**：过滤无效订单，使用 `SKU Subtotal After Discount` 作为销售额（去运费），通过 Seller SKU 或 标题 匹配。
*   **核心流程**：
    *   **每日上传**：录入昨日新订单。
    *   **每周更新**：周一上传过去7天数据，更新订单状态（如转为完成、退货）。
    *   **交互**：上传 -> 解析 -> 自动匹配 (标题/SKU) -> 弹窗人工确认/修正 -> 批量提交 (Upsert)。
*   **匹配策略**：针对“同产品多链接”场景，优先使用**商品标题**进行匹配，并建立映射记忆机制。

## 2. 可行性分析
**结论：方案整体高度可行，逻辑清晰，符合业务需求。**

*   **数据支撑**：提供的样本文件 (`TK-7.xlsx`, `xp-1.xlsx`) 包含了方案所需的所有关键字段（订单号、状态、SKU、标题、各项金额细分）。
*   **业务逻辑**：“每日新增 + 周一更新”的策略有效解决了电商订单状态流转（如发货->完成/退货）的滞后性问题。
*   **技术实现**：
    *   Excel 解析与指纹识别（表头校验）技术成熟。
    *   Upsert（存在则更新，不存在则插入）是标准数据库操作。
    *   前端预览与手动修正流程能有效处理数据异常，保证入库准确性。

## 3. 建议与优化
1.  **金额字段严格定义**：
    *   **Shopee**：确认使用 `Total Harga Produk`。样本数据显示 `Harga Setelah Diskon` 确实是单价，若数量>1 会导致金额错误，方案中的纠正非常关键。
    *   **TikTok**：确认使用 `SKU Subtotal After Discount`。样本中 `Order Amount` 包含运费，直接使用会导致 GMV 虚高。
2.  **匹配机制增强**：
    *   仅靠“标题”匹配可能因平台截断或修改而不稳定。建议引入 **`ListingMapping` 表**（或在现有表中增加别名列），记录 `(Platform, ExternalTitle/ExternalSKU) -> InternalListingId` 的关系。
    *   当用户在前端手动修正匹配关系后，系统应自动保存这条映射，下次自动应用，实现“越用越聪明”。
3.  **状态映射表**：
    *   建议在代码中维护一份明确的 `Status Mapping` 配置，将 Shopee/TikTok 各自繁杂的状态（如 `Perlu Dikirim`, `Awaiting Shipment`）统一映射为系统内部状态（如 `PENDING`, `SHIPPED`, `COMPLETED`, `RETURNED`, `CANCELLED`）。
4.  **性能考虑**：
    *   周一上传7天数据时，数据量可能较大。建议后端采用批量处理（Batch Processing），避免一次性大事务导致超时。

## 4. 阶段性实施步骤

### 第一阶段：数据库与后端基础 (预计 1-2 天)
1.  **数据库迁移**：
    *   修改 `SalesData` Schema，添加 `platformOrderId` (Unique), `orderStatus`, `quantity`, `unitPrice` 等字段。
    *   创建或更新 `StoreProductListing` 以支持标题匹配/别名映射。
2.  **后端解析服务**：
    *   实现 `ExcelParser` 类，分别处理 Shopee 和 TikTok 格式。
    *   实现“指纹识别”逻辑，自动判断文件类型或校验用户选择。
    *   实现数据清洗逻辑（去除未付款/取消订单，计算正确金额）。

### 第二阶段：核心业务逻辑 (预计 2-3 天)
1.  **匹配算法实现**：
    *   实现 `ListingMatcher` 服务：输入 (平台, 标题, SKU) -> 输出 (ListingId, 匹配置信度)。
    *   逻辑：精确 SKU 匹配 -> 精确标题匹配 -> 模糊/别名匹配 -> 失败。
2.  **数据入库逻辑 (Upsert)**：
    *   实现 `bulkUpsert` 接口：接收前端确认后的数据。
    *   逻辑：按 `platformOrderId` 查找 -> 存在则更新状态/金额 -> 不存在则插入。

### 第三阶段：前端交互开发 (预计 3-4 天)
1.  **上传界面**：
    *   文件拖拽上传区，增加“平台选择”下拉框（带自动检测提示）。
2.  **预览与确认组件**：
    *   开发数据表格，展示解析结果。
    *   **关键功能**：对于匹配失败的行，提供下拉搜索框，允许用户搜索并选择系统内的 Listing。
    *   高亮显示异常数据（如金额为0，状态未知）。

### 第四阶段：测试与验证 (预计 1-2 天)
1.  **单元测试**：针对各种边缘情况的 Excel 数据进行解析测试。
2.  **集成测试**：模拟“周一更新”场景，验证旧订单状态是否正确翻转（如从 Shipped 变为 Returned），且销售额是否正确扣除。
3.  **用户验收**：使用真实历史数据进行试运行。
